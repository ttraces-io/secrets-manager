[workspace]
members = [
    "enclave",
    "host",
    "gui",
    "cli",
    "desktop",
    "devtest",
]
exclude = [
    "fuzz",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "thin"
panic = "abort"
strip = "debuginfo"

[profile.dev]
panic = "abort"

