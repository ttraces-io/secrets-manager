name: CI & Test Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # Rust Workspace Build, Test, Clippy, and Formatting
  # ---------------------------------------------------------------------------
  rust-workspace:
    name: Rust Workspace — Build, Test, Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Linux GUI & Crypto Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev libx11-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev libgtk-3-dev

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build workspace (all targets)
        run: cargo build --workspace --all-targets

      - name: Run Core Test Suite
        run: cargo test --workspace -- --nocapture

      - name: Run DevTest Verification Harness
        run: cargo test -p traces-sm-devtest -- --nocapture

      - name: Report Ignored Conformance Gaps
        if: always()
        run: cargo test --workspace -- --ignored --list || true

      - name: Clippy (workspace)
        run: cargo clippy --workspace --all-targets -- -A warnings

      - name: Formatting check
        run: cargo fmt --all -- --check

  # ---------------------------------------------------------------------------
  # Supply chain security audit
  # ---------------------------------------------------------------------------
  rust-supply-chain:
    name: Rust Supply Chain Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Known-vulnerability audit
        run: cargo audit --ignore RUSTSEC-2020-0071 || true

  # ---------------------------------------------------------------------------
  # Python Bounty Bot Test Suite
  # ---------------------------------------------------------------------------
  python-bounty-bot:
    name: Bounty Bot Test & Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install uv and Setup Python
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true

      - name: Install Bounty Bot Dependencies
        run: |
          cd bounty_bot
          uv sync

      - name: Run Pytest Suite
        run: |
          cd bounty_bot
          uv run pytest

  # ---------------------------------------------------------------------------
  # Cross-Platform Matrix (Linux, Windows, macOS)
  # ---------------------------------------------------------------------------
  cross-platform-matrix:
    name: Multi-Platform Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            cli_artifact: traces-sm-cli-linux
            desktop_artifact: traces-sm-desktop-linux
          - os: windows-latest
            cli_artifact: traces-sm-cli-windows.exe
            desktop_artifact: traces-sm-desktop-windows.exe
          - os: macos-latest
            cli_artifact: traces-sm-cli-macos
            desktop_artifact: traces-sm-desktop-macos

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libx11-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev libssl-dev libgtk-3-dev

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2

      - name: Build CLI & Desktop Binaries
        run: |
          cargo build --release -p traces-sm -p traces-sm-desktop

      - name: Run DevTest on ${{ matrix.os }}
        run: |
          cargo test -p traces-sm-devtest -- --nocapture

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.os }}
          path: |
            target/release/traces-sm
            target/release/traces-sm.exe
            target/release/traces-sm-desktop
            target/release/traces-sm-desktop.exe
          if-no-files-found: warn

  # ---------------------------------------------------------------------------
  # Continuous Fuzz Testing (cargo-fuzz / libFuzzer)
  # ---------------------------------------------------------------------------
  cargo-fuzz-testing:
    name: Continuous Fuzz Testing (libFuzzer)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Linux Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev

      - name: Setup Nightly Rust Toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          targets: x86_64-unknown-linux-gnu
          components: rust-src, llvm-tools-preview

      - name: Cache Cargo & Fuzz Dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-fuzz
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-fuzz

      - name: Fuzz ASN.1 / PKCS#8 Key Parsing
        run: cargo +nightly fuzz run --target x86_64-unknown-linux-gnu fuzz_asn1 -- -max_total_time=15 -max_len=4096

      - name: Fuzz JSON-RPC & REST Deserialization
        run: cargo +nightly fuzz run --target x86_64-unknown-linux-gnu fuzz_json_rpc -- -max_total_time=15 -max_len=8192

      - name: Fuzz Base64 & Base64URL Decoders
        run: cargo +nightly fuzz run --target x86_64-unknown-linux-gnu fuzz_base64 -- -max_total_time=15 -max_len=4096

  # ---------------------------------------------------------------------------
  # Web GUI & UI Test Suite (React / Vite / Vitest)
  # ---------------------------------------------------------------------------
  web-gui-tests:
    name: Web GUI — Build & UI Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: gui/package-lock.json

      - name: Install GUI Dependencies
        run: |
          cd gui
          npm ci

      - name: Run React UI Test Suite
        run: |
          cd gui
          npm test

      - name: Build Web GUI Frontend
        run: |
          cd gui
          npm run build
